## 归并排序快速求逆序对数
因为归并排序是把已经有序的数组合并,有序本身是有优势的,如果一个数比另一个数组的最小的数要小,那么它小于这个数组的所有数
它避免了逐个比较,所以复杂度不是n ^ 2 而是n log n


我需要统计很多个“大于关系”，但是我不想一个一个判断。

## 07 CF2023A Concatenation of Arrays

两个块 A、B，谁在前面更好。然后不要每次都算四个比较，而是问：这个比较结果能不能被一个更简单的状态概括？

### Q:可是我在赛时不太可能想到那种特殊的数据例子吧,所以我不太能判断传递性.这时候应该怎么做?是因为如果这是一个二元组,就需要写一个递进的比较逻辑吗?也就是有种字典序的感觉,虽然逻辑和最初的比较逻辑相近,但是它更加稳定.为什么一定是先比较最小值,如果先比较最大值再比较最小值呢?我觉得是ok的
竞赛里很多排序题最后都不是写一个玄学比较器，而是找一个稳定的键：
第一关键字
第二关键字
必要时第三关键字