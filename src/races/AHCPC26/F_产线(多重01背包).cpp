#include <iostream>
#include <vector>
struct line
{
    int a, b, c, d;
};
int main()
{
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    line aim_line(a, b, c, d);
    int n;
    std::cin >> n;
}