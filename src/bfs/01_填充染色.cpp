#include <iostream>
const char sh = '#';
const char ze = '0';
const int maxn = 500;
char map[maxn][maxn];
int n, m;
int main()
{
    std::cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
        }
    }
}